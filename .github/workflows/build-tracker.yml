name: Build Tracker (.NET 8)

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'SITECOM/Tracker/**'
      - '.github/workflows/build-tracker.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'SITECOM/Tracker/**'
  workflow_dispatch:

jobs:

  pre-actions:
    name: Pre Actions
    runs-on: ubuntu-latest
    steps:
      - name: Contexto
        run: |
          echo "🔧 Iniciando pipeline Tracker..."
          echo "📦 Job executável para rastreamento de pedidos"

  open-ssh-port:
    name: Abrir porta SSH AWS (22)
    runs-on: ubuntu-latest
    needs: pre-actions
    steps:
      - name: Configurar credenciais AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Abrir porta SSH temporariamente
        run: |
          aws ec2 authorize-security-group-ingress \
            --group-id ${{ secrets.BASTION_SG_ID }} \
            --protocol tcp --port 22 --cidr 0.0.0.0/0 || echo "⚠️ Regra já existente"

  build:
    name: Build (.NET 8)
    runs-on: ubuntu-latest
    needs: pre-actions
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Instalar .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restaurar dependências
        working-directory: SITECOM/Tracker
        run: dotnet restore Tracker.csproj

      - name: Compilar o projeto
        working-directory: SITECOM/Tracker
        run: dotnet build Tracker.csproj --configuration Release --no-restore

      - name: Publicar (gera o executável pronto para Linux)
        working-directory: SITECOM/Tracker
        run: dotnet publish Tracker.csproj -c Release -r linux-x64 -o ../../publish-tracker --self-contained false

      - name: Verificar se appsettings.json foi copiado
        run: |
          if [ -f "publish-tracker/appsettings.json" ]; then
            echo "✅ appsettings.json encontrado no publish"
            ls -lh publish-tracker/appsettings.json
          else
            echo "⚠️ appsettings.json NÃO encontrado no publish!"
            echo "Copiando do diretório fonte..."
            cp SITECOM/Tracker/appsettings.json publish-tracker/appsettings.json
            echo "✅ appsettings.json copiado"
          fi

      - name: Remover appsettings de desenvolvimento
        run: |
          if [ -f "publish-tracker/appsettings.Development.json" ]; then
            rm -f publish-tracker/appsettings.Development.json
            echo "✅ appsettings.Development.json removido"
          fi

      - name: Criar arquivo de informações do build
        run: |
          cat > publish-tracker/BUILD_INFO.txt <<EOF
          Build: ${{ github.run_number }}
          Commit: ${{ github.sha }}
          Branch: ${{ github.ref_name }}
          Data: $(date -u +"%Y-%m-%d %H:%M:%S UTC")
          Autor: ${{ github.actor }}
          EOF

      - name: Compactar o resultado
        run: |
          cd publish-tracker
          zip -r ../tracker.zip .
          cd ..
          ls -lh tracker.zip

      - name: Armazenar artefato no GitHub
        uses: actions/upload-artifact@v4
        with:
          name: Tracker
          path: tracker.zip
          retention-days: 30

  transfer-bastion:
    name: Transfer para Bastion
    runs-on: ubuntu-latest
    needs: [ build, open-ssh-port ]
    steps:
      - name: Aguardar propagação da regra SSH
        run: |
          echo "⏳ Aguardando 10 segundos para garantir que a porta SSH está aberta..."
          sleep 10
          echo "✅ Prosseguindo com o transfer"
      
      - name: Testar conectividade SSH
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ secrets.BASTION_HOST }}
          username: ${{ secrets.BASTION_USERNAME }}
          key: ${{ secrets.BASTION_SSH_KEY }}
          script: |
            echo "✅ Conectado no Bastion com sucesso!"
            whoami
            pwd
            ls -la /home/ec2-user/deploys/ || mkdir -p /home/ec2-user/deploys/
      
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: Tracker

      - name: Verificar arquivo baixado
        run: |
          echo "=== Conteúdo do diretório atual ==="
          pwd
          ls -la
          echo ""
          echo "=== Verificando arquivo específico ==="
          if [ -f "tracker.zip" ]; then
            ls -lh tracker.zip
            echo "✅ Arquivo encontrado!"
          else
            echo "❌ Arquivo tracker.zip NÃO encontrado!"
            echo "Procurando arquivos .zip:"
            find . -name "*.zip" -type f
          fi

      - name: Enviar artefato para o Bastion
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.BASTION_HOST }}
          username: ${{ secrets.BASTION_USERNAME }}
          key: ${{ secrets.BASTION_SSH_KEY }}
          source: "tracker.zip"
          target: "/home/ec2-user/deploys/"

  deploy-bastion:
    name: Deploy no Bastion
    runs-on: ubuntu-latest
    needs: transfer-bastion
    steps:
      - name: Descompactar e configurar no Bastion
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ secrets.BASTION_HOST }}
          username: ${{ secrets.BASTION_USERNAME }}
          key: ${{ secrets.BASTION_SSH_KEY }}
          script_stop: false
          script: |
            set -e
            echo "=========================================="
            echo "  Deploy Tracker no Bastion"
            echo "=========================================="
            
            DEPLOY_ZIP="/home/ec2-user/deploys/tracker.zip"
            TARGET_DIR="$HOME/Tracker"
            BACKUP_DIR="$HOME/Tracker-backup-$(date +%Y%m%d-%H%M%S)"
            
            # Verificar se arquivo foi enviado
            if [ ! -f "$DEPLOY_ZIP" ]; then
              echo "❌ Arquivo $DEPLOY_ZIP não encontrado!"
              exit 1
            fi
            
            echo "✅ Arquivo encontrado: $DEPLOY_ZIP"
            ls -lh "$DEPLOY_ZIP"
            
            # Fazer backup da versão anterior (se existir)
            if [ -d "$TARGET_DIR" ]; then
              echo "📦 Criando backup da versão anterior..."
              sudo cp -r "$TARGET_DIR" "$BACKUP_DIR"
              echo "✅ Backup criado em: $BACKUP_DIR"
            fi
            
            # Criar diretório de destino
            echo "📁 Preparando diretório $TARGET_DIR..."
            mkdir -p "$TARGET_DIR"
            
            # Limpar arquivos antigos
            echo "🧹 Limpando arquivos antigos..."
            rm -rf "$TARGET_DIR"/*
            
            # Extrair novo pacote
            echo "📦 Extraindo pacote..."
            unzip -o "$DEPLOY_ZIP" -d "$TARGET_DIR"
            
            # Verificar se appsettings.json foi extraído
            if [ ! -f "$TARGET_DIR/appsettings.json" ]; then
              echo "⚠️ appsettings.json não encontrado no pacote! Criando um básico..."
              printf '{\n  "ConnectionString": "",\n  "UseSecretsManager": true,\n  "SecretArn": "arn:aws:secretsmanager:us-east-1:615283740315:secret:prod/sqlserver/ecom-QABqVU",\n  "DynamoDB": {\n    "TableName": "tracker-pedidos",\n    "Region": "us-east-1"\n  },\n  "Correios": {\n    "Usuario": "",\n    "SecretKey": "",\n    "CartaPostal": "0078034701",\n    "CepOrigem": "01310100"\n  },\n  "EmailService": {\n    "BaseUrl": "https://lion.aquanimal.com.br/ajax/OrderStatusAjaxHandler.ashx"\n  }\n}' > "$TARGET_DIR/appsettings.json"
            else
              echo "✅ appsettings.json encontrado no pacote e será usado"
            fi
            
            # Ajustar permissões
            echo "🔐 Ajustando permissões..."
            chmod +x "$TARGET_DIR/Tracker"
            
            # Verificar instalação
            echo ""
            echo "=========================================="
            echo "  ✅ Deploy concluído com sucesso!"
            echo "=========================================="
            echo ""
            echo "📂 Localização: $TARGET_DIR"
            echo "📋 Arquivos instalados:"
            ls -lh "$TARGET_DIR" | head -20
            echo ""
            
            # Mostrar informações do build
            if [ -f "$TARGET_DIR/BUILD_INFO.txt" ]; then
              echo "📝 Informações do Build:"
              cat "$TARGET_DIR/BUILD_INFO.txt"
              echo ""
            fi
            
            # Verificar se .NET está instalado
            if command -v dotnet &> /dev/null; then
              echo "✅ .NET Runtime instalado: $(dotnet --version)"
            else
              echo "⚠️ .NET Runtime não encontrado!"
              echo "   Instale com: sudo dnf install dotnet-runtime-8.0 -y"
            fi
            
            echo ""
            echo "=========================================="
            echo "  📋 Como executar:"
            echo "=========================================="
            echo "  cd ~/Tracker"
            echo "  dotnet Tracker.dll"
            echo ""
            echo "  Ou configure no cron:"
            echo "  0 */6 * * * cd ~/Tracker && /usr/bin/dotnet Tracker.dll >> ~/Tracker/tracker.log 2>&1"
            echo "  (Executa a cada 6 horas)"
            echo "=========================================="

  close-ssh-port:
    name: Fechar porta SSH AWS (22)
    runs-on: ubuntu-latest
    needs: [ open-ssh-port, deploy-bastion ]
    if: always()
    steps:
      - name: Configurar credenciais AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Fechar porta SSH
        run: |
          aws ec2 revoke-security-group-ingress \
            --group-id ${{ secrets.BASTION_SG_ID }} \
            --protocol tcp --port 22 --cidr 0.0.0.0/0 || echo "⚠️ Regra já removida"

  post-actions:
    name: Post Actions
    runs-on: ubuntu-latest
    needs: [ build, transfer-bastion, deploy-bastion, close-ssh-port ]
    if: always()
    steps:
      - name: Resumo
        run: |
          echo "=========================================="
          echo "  🏁 Pipeline Tracker Finalizado"
          echo "=========================================="
          echo ""
          echo "📊 Status dos Jobs:"
          echo "  build: ${{ needs.build.result }}"
          echo "  transfer-bastion: ${{ needs['transfer-bastion'].result }}"
          echo "  deploy-bastion: ${{ needs['deploy-bastion'].result }}"
          echo "  close-ssh-port: ${{ needs['close-ssh-port'].result }}"
          echo ""
          echo "=========================================="
          
          if [ "${{ needs.build.result }}" == "success" ] && \
             [ "${{ needs['transfer-bastion'].result }}" == "success" ] && \
             [ "${{ needs['deploy-bastion'].result }}" == "success" ]; then
            echo "✅ Deploy concluído com sucesso!"
            echo "📂 Aplicação disponível em: ~/Tracker"
            echo "🚀 Execute manualmente ou configure no cron"
          else
            echo "❌ Pipeline teve falhas. Verifique os logs acima."
          fi

